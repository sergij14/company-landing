{
  "name": "template-task",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "live-server": "^1.2.1"
  },
  "scripts": {
    "start": "npm run build && live-server src --port=8080",
    "build": "postcss styles.css -o src/styles.css"
  },
  "dependencies": {
    "autoprefixer": "^10.2.4",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.1.4"
  }
}
