{
  "name": "template-task",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "live-server": "^1.2.1"
  },
  "scripts": {
    "build": "postcss src/styles.css -o dist/styles.css",
    "dev": "postcss src/styles.css -o dist/styles.css -w --verbose",
    "server": "live-server dist --port=8080",
    "start": "concurrently \"npm run dev\" \"npm run server\""
  },
  "dependencies": {
    "autoprefixer": "^10.2.4",
    "concurrently": "^7.0.0",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.2.19"
  }
}
